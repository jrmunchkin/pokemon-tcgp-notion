{
  "name": "express-server",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "ts-node-dev --transpile-only --no-notify --exit-child src/index.ts",
    "start": "nodemon src/index.ts",
    "build": "rimraf dist && tsc",
    "ts.check": "tsc --project tsconfig.json",
    "test": "echo \"Error: no test specified\" && exit 1",
    "tcg-sync": "ts-node src/tcg-fetcher.ts",
    "tcg-sync:build": "npm run build && node dist/tcg-fetcher.js"
  },
  "devDependencies": {
    "@types/node": "^18.15.5",
    "rimraf": "^4.4.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.0.2"
  },
  "dependencies": {
    "@types/cors": "^2.8.13",
    "@types/express": "^4.17.17",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "axios": "^1.1.2",
    "@notionhq/client": "^2.2.1"
  }
}
